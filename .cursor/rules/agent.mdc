---
alwaysApply: true
---

# What? 
List of instructions for the agent

## Important Instructions
- Always source the virtual environment using `/ssd/.venv-lerobot/bin/activate` before running any commands in the terminal.
- Do not make any changes to the codebase unless explicitly instructed to do so.
- `wandb` API keys are stored in `~/.netrc`.
- For any request about training status, progress, steps, metrics, or run health, use the `wandb` mcp server by default (without waiting for explicit user instruction).
- Proactively use the `context7` mcp server to query official docs.
- When fixing errors, carefully analyze the error message and the stack trace to identify the root cause.
- Always test the script after making changes.
- If a command fails due to permission or network issues (e.g. sandbox blocking network or file access), re-run it outside the sandbox (e.g. with `required_permissions: ["network"]` or `["all"]` as appropriate).
- When running scripts that access external resources (HuggingFace datasets, network connections) or require display/GUI capabilities (rerun visualization, OpenCV camera access), use `required_permissions: ["all"]` from the start to avoid permission failures.



## context7
Some important libraries that you might want to use => 
- /huggingface/lerobot
- /wandb/wandb
